
<%
    contacts = Contact.all
    companies = Company.all
    companies_name = Company.find_by({"id" => contact["company_id"]})
   
%>


<h1> Contacts
</h1>

<table>
<tr>
    <th>First Name</th>
    <th>Last Name</th>
    <th>Email</th>
</tr>
<% for contact in contacts %>
<tr>
    <td><%= contact["first_name"]%></td>
    <td><%= contact["last_name"]%></td>
    <td><%= contact["email"]%></td>
</tr>
<%end%>

<p>
</p>